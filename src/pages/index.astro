---
import Layout from '../layouts/Layout.astro';
import Pricing from '../components/Pricing.astro';
import Contact from '../components/Contact.astro';

const NICHES = [
  {
    id: "medico", label: "Médicos", name: "Dra. Marina Vega", sub: "Cardiología · CDMX",
    bg: "#F4F7FB", primary: "#0E5BA8", soft: "#E2ECFA", ink: "#0B2447",
    headline: "Cuidamos tu corazón con la ciencia más actual.",
    cta: "Agendar consulta",
    services: ["Ecocardiograma", "Holter 24h", "Prueba de esfuerzo", "Consulta general"],
    badge: "Cédula 4582901",
  },
  {
    id: "dental", label: "Dentistas", name: "Sonrisa Clínica", sub: "Odontología integral",
    bg: "#F2F8F8", primary: "#0E9E96", soft: "#DDEEEC", ink: "#0A3F3D",
    headline: "Sonrisas que cambian la manera en que ves el mundo.",
    cta: "Reservar limpieza",
    services: ["Blanqueamiento", "Ortodoncia", "Implantes", "Diseño de sonrisa"],
    badge: "Tecnología 3D",
  },
  {
    id: "legal", label: "Abogados", name: "Herrera & Asociados", sub: "Derecho corporativo",
    bg: "#F6F4EE", primary: "#1B2236", soft: "#E8E2D2", ink: "#10162A",
    headline: "Defendemos tu negocio como si fuera el nuestro.",
    cta: "Solicitar consulta",
    services: ["Mercantil", "Familiar", "Penal", "Migratorio"],
    badge: "20 años de práctica",
  },
  {
    id: "shop", label: "Ecommerce", name: "Botanique Tienda", sub: "Belleza natural",
    bg: "#FAF4EC", primary: "#7A4B2A", soft: "#F0E2CC", ink: "#3A2614",
    headline: "Cosmética artesanal para sentirte como tú.",
    cta: "Comprar ahora",
    services: ["Skincare", "Aromaterapia", "Cabello", "Cuerpo"],
    badge: "Envío gratis $899",
  },
];

const PASOS = [
  { n: "01", t: "Te escuchamos",     b: "Una llamada de 30 minutos. Nos cuentas tu negocio, tu cliente y a dónde quieres llegar.", icon: "💬" },
  { n: "02", t: "Diseñamos la idea", b: "Recibes un primer concepto en 48 horas. Lo ajustamos juntos hasta que sientas que es tuyo.", icon: "✶" },
  { n: "03", t: "Construimos rápido", b: "Programamos tu sitio con código limpio, optimizado para Google y listo para escalar.", icon: "⚙" },
  { n: "04", t: "Publicamos y cuidamos", b: "Lo lanzamos en tu dominio, te enseñamos a editarlo y seguimos contigo el primer mes sin costo.", icon: "✓" },
];

const TESTIMONIOS = [
  { q: "Pasé de no aparecer en Google a recibir 3 pacientes nuevos por semana. Mi clínica creció sin que yo gastara en publicidad cada mes.", a: "Dra. Marina V.", r: "Cardióloga · CDMX" },
  { q: "Antes peleaba con plantillas que no entendían lo que vendo. Kreatu se sentó conmigo, entendió mi marca y la página quedó tal cual la imaginé.", a: "Carla M.", r: "Botanique Tienda" },
  { q: "Levantamos el sitio de la firma en 8 días. Profesional, limpio, y nuestros clientes corporativos lo notaron de inmediato.", a: "Lic. Ricardo H.", r: "Herrera & Asociados" },
  { q: "El soporte por WhatsApp es lo que más valoro. Cualquier ajuste lo resuelven el mismo día.", a: "Dr. Ángel S.", r: "Sonrisa Clínica" },
];

const FAQ_ITEMS = [
  { q: "¿En cuánto tiempo entregan mi página?", a: "Entre 5 y 10 días para Starter y Pro. Pro Plus normalmente toma 2 a 3 semanas, dependiendo de los productos y métodos de pago." },
  { q: "¿Puedo editar el sitio yo después?", a: "Sí. Te dejamos un editor visual y una guía corta en video. Si prefieres, nosotros lo seguimos manteniendo." },
  { q: "¿El precio incluye dominio y hosting?", a: "Incluimos el dominio el primer año y SSL. El hosting tiene un costo desde $149/mes según el tipo de sitio. Te explicamos antes de empezar." },
  { q: "¿Qué pasa si no me gusta el primer diseño?", a: "Está incluido: ajustamos hasta que sientas que es tuyo. La mayoría de nuestros clientes lo aprueba en la segunda revisión." },
  { q: "¿Hacen sitios solo para estos 4 nichos?", a: "Estos son los más frecuentes, pero también trabajamos con estética, restaurantes, coaches, inmobiliarias y más. Escríbenos." },
  { q: "¿Pueden migrar mi página actual?", a: "Sí. Auditamos lo que tienes, conservamos lo que sirve y rediseñamos lo demás. El SEO no se pierde — lo cuidamos." },
];
---

<Layout
  title="Diseño de Página Web Profesional y Económica en México | KreatuSitioWeb"
  description="Diseñamos páginas web profesionales para médicos, dentistas, abogados y tiendas en México. Desde $999.99 MXN, entrega en 7 días, SSL incluido."
>

<!-- ═══════════════════ HERO ═══════════════════ -->
<section class="kt-hero" id="top">
  <div class="kt-hero__bg" aria-hidden="true">
    <div class="kt-hero__grid"></div>
    <div class="kt-hero__glow"></div>
  </div>

  <div class="kt-shell kt-hero__inner">
    <div class="kt-hero__copy">
      <div class="kt-eyebrow">
        <span class="kt-eyebrow__dot"></span>
        <span>Estudio de páginas web · MX</span>
        <span class="kt-eyebrow__sep">/</span>
        <span class="kt-mono">desde $999.99</span>
      </div>
      <h1 class="kt-hero__title">
        Páginas web<br/>
        que <em class="kt-italic">trabajan</em><br/>
        por ti, <span class="kt-underline">no al revés.</span>
      </h1>
      <p class="kt-hero__lede">
        Diseñamos sitios profesionales para <em>doctores, dentistas, abogados</em> y <em>tiendas en línea</em>.
        Escuchamos tu negocio, lo entendemos y lo convertimos en una página que vende — sin plantillas, sin complicaciones.
      </p>
      <div class="kt-hero__ctas">
        <a href="#contacto" class="kt-btn kt-btn--primary kt-btn--lg">
          Quiero mi página
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M5 12h14M13 5l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="#demo" class="kt-btn kt-btn--ghost kt-btn--lg">Ver demo en vivo</a>
      </div>
      <ul class="kt-hero__proof">
        <li><strong>+120</strong> sitios entregados</li>
        <li><strong>7 días</strong> en promedio</li>
        <li><strong>100%</strong> responsive</li>
        <li><strong>SSL</strong> incluido</li>
      </ul>
    </div>

    <div class="kt-hero__demo">
      <div class="kt-demoFrame">
        <div class="kt-demoFrame__chrome">
          <div class="kt-demoFrame__dots"><i></i><i></i><i></i></div>
          <div class="kt-demoFrame__url">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none"><path d="M12 2a10 10 0 100 20 10 10 0 000-20zm0 18c-4.4 0-8-3.6-8-8s3.6-8 8-8 8 3.6 8 8-3.6 8-8 8z" fill="currentColor"/></svg>
            <span id="demo-url">medico.kreatusitioweb.com</span>
          </div>
          <div class="kt-demoFrame__live"><span class="kt-pulse"></span> en vivo</div>
        </div>
        <div class="kt-demoFrame__viewport" id="demo-viewport"></div>
      </div>
      <div class="kt-demoSwitcher" role="tablist">
        {NICHES.map((n, i) => (
          <button role="tab" data-niche={i} class:list={["kt-demoSwitcher__tab", { "is-on": i === 0 }]}>
            <span class="kt-demoSwitcher__bar"></span>
            {n.label}
          </button>
        ))}
      </div>
      <div class="kt-demoCaption">
        <span class="kt-mono">// el mismo equipo · 4 industrias · 4 mundos visuales</span>
      </div>
    </div>
  </div>

  <!-- Marquee -->
  <div class="kt-marquee" aria-hidden="true">
    <div class="kt-marquee__track">
      {["Doctores","Dentistas","Abogados","Ecommerce","Estética","Restaurantes","Coaches","Inmobiliarias","Veterinarias","Contadores","Doctores","Dentistas","Abogados","Ecommerce","Estética","Restaurantes","Coaches","Inmobiliarias","Veterinarias","Contadores"].map(it => (
        <span class="kt-marquee__item"><span class="kt-marquee__star">✦</span>{it}</span>
      ))}
    </div>
  </div>
</section>


<!-- ═══════════════════ NICHOS ═══════════════════ -->
<section class="kt-nichos" id="nichos">
  <div class="kt-shell">
    <div class="kt-section__head">
      <div class="kt-eyebrow"><span class="kt-eyebrow__dot"></span><span class="kt-mono">01 / Nichos</span></div>
      <h2 class="kt-section__title">Una página <em class="kt-italic">hecha para tu industria</em>, no una plantilla genérica.</h2>
      <p class="kt-section__lede">Cada profesión vende distinto. Por eso adaptamos estructura, copy y diseño al lenguaje que tus pacientes, clientes o compradores ya conocen.</p>
    </div>
    <div class="kt-nichos__grid">
      {[
        { emoji:"🩺", title:"Para médicos",   body:"Agenda en línea, perfil profesional, blog de salud y página de servicios optimizada para búsqueda local.", tag:"Cardiología, dermatología, pediatría…" },
        { emoji:"🦷", title:"Para dentistas", body:"Antes y después, planes de tratamiento, facilidades de pago y reservas integradas a WhatsApp.", tag:"Ortodoncia, estética, integral…" },
        { emoji:"⚖️", title:"Para abogados",  body:"Casos de éxito, áreas de práctica, primer contacto confidencial y formularios con encriptación.", tag:"Mercantil, familiar, penal…" },
        { emoji:"🛍",  title:"Para tu tienda", body:"Catálogo, carrito, métodos de pago en MXN, envíos y panel para subir productos sin programar.", tag:"Belleza, moda, comida, joyería…" },
      ].map(c => (
        <a href="#demo" class="kt-nicho">
          <div class="kt-nicho__num">{c.emoji}</div>
          <h3 class="kt-nicho__title">{c.title}</h3>
          <p class="kt-nicho__body">{c.body}</p>
          <div class="kt-nicho__tag kt-mono">{c.tag}</div>
          <div class="kt-nicho__arrow">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M7 17L17 7M9 7h8v8" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>


<!-- ═══════════════════ BENEFICIOS BENTO ═══════════════════ -->
<section class="kt-bento" id="beneficios">
  <div class="kt-shell">
    <div class="kt-section__head kt-section__head--row">
      <div>
        <div class="kt-eyebrow"><span class="kt-eyebrow__dot"></span><span class="kt-mono">02 / Beneficios</span></div>
        <h2 class="kt-section__title">Profesional sin ser <em class="kt-italic">complicado</em>.</h2>
      </div>
      <p class="kt-section__lede kt-section__lede--side">Diseño que se ve serio, código que carga rápido, copy que vende. Y un equipo humano que entiende lo que haces.</p>
    </div>
    <div class="kt-bento__grid">
      <div class="kt-bento__cell kt-bento__cell--big">
        <div class="kt-bento__head"><span class="kt-mono">// rendimiento</span><h3>Carga en menos de <em>2 segundos</em>.</h3></div>
        <div class="kt-perf">
          <div class="kt-perf__row"><span>LCP</span><div class="kt-perf__bar"><i style="width:22%"></i></div><span class="kt-mono">1.4s</span></div>
          <div class="kt-perf__row"><span>CLS</span><div class="kt-perf__bar"><i style="width:6%"></i></div><span class="kt-mono">0.02</span></div>
          <div class="kt-perf__row"><span>FID</span><div class="kt-perf__bar"><i style="width:12%"></i></div><span class="kt-mono">12ms</span></div>
          <div class="kt-perf__row"><span>SEO</span><div class="kt-perf__bar"><i style="width:96%"></i></div><span class="kt-mono">98/100</span></div>
        </div>
      </div>
      <div class="kt-bento__cell">
        <span class="kt-mono">// SEO</span>
        <h3>Te encuentran en Google.</h3>
        <p>Estructura, metadatos y velocidad pensados para que aparezcas cuando alguien busca tu servicio cerca.</p>
        <div class="kt-search">
          <div class="kt-search__bar">🔍 dentista en guadalajara</div>
          <div class="kt-search__result"><strong>Sonrisa Clínica</strong><span> · kreatu</span></div>
        </div>
      </div>
      <div class="kt-bento__cell">
        <span class="kt-mono">// móvil</span>
        <h3>Se ve perfecto en cualquier pantalla.</h3>
        <div class="kt-devices">
          <div class="kt-devices__phone"><div></div><div class="kt-devices__line"></div><div class="kt-devices__line short"></div></div>
          <div class="kt-devices__tablet"><div></div><div></div><div></div></div>
        </div>
      </div>
      <div class="kt-bento__cell kt-bento__cell--accent">
        <span class="kt-mono">// soporte</span>
        <h3>Hablamos contigo, no con un bot.</h3>
        <p>Acompañamiento por WhatsApp y reuniones cuando lo necesites. Cambios incluidos los primeros 30 días.</p>
        <div class="kt-chat">
          <div class="kt-chat__msg kt-chat__msg--in">¿Puedo cambiar la foto del banner?</div>
          <div class="kt-chat__msg kt-chat__msg--out">Claro 👋 mándame la nueva y queda hoy mismo.</div>
        </div>
      </div>
      <div class="kt-bento__cell kt-bento__cell--wide">
        <div class="kt-bento__head"><span class="kt-mono">// resultados</span><h3>Más visitas, más llamadas, más ventas.</h3></div>
        <div class="kt-stats">
          <div class="kt-stat"><div class="kt-stat__num">3.4×</div><div class="kt-stat__lbl">más leads en 60 días</div></div>
          <div class="kt-stat"><div class="kt-stat__num">68%</div><div class="kt-stat__lbl">tráfico desde móvil</div></div>
          <div class="kt-stat"><div class="kt-stat__num">7d</div><div class="kt-stat__lbl">tu sitio publicado</div></div>
          <div class="kt-stat"><div class="kt-stat__num">$0</div><div class="kt-stat__lbl">costos ocultos</div></div>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ═══════════════════ PROCESO ═══════════════════ -->
<section class="kt-proc" id="proceso">
  <div class="kt-shell">
    <div class="kt-section__head">
      <div class="kt-eyebrow"><span class="kt-eyebrow__dot"></span><span class="kt-mono">03 / Proceso</span></div>
      <h2 class="kt-section__title">De la idea a <em class="kt-italic">tu primer cliente</em> en internet, en 4 pasos.</h2>
    </div>
    <div class="kt-proc__rail">
      <div class="kt-proc__line"></div>
      {PASOS.map(p => (
        <div class="kt-procStep">
          <div class="kt-procStep__node"><span class="kt-procStep__icon">{p.icon}</span></div>
          <div class="kt-procStep__num kt-mono">{p.n}</div>
          <h3 class="kt-procStep__title">{p.t}</h3>
          <p class="kt-procStep__body">{p.b}</p>
        </div>
      ))}
    </div>
  </div>
</section>


<!-- ═══════════════════ DEMO INTERACTIVA ═══════════════════ -->
<section class="kt-demo" id="demo">
  <div class="kt-shell">
    <div class="kt-section__head">
      <div class="kt-eyebrow"><span class="kt-eyebrow__dot"></span><span class="kt-mono">04 / Demo en vivo</span></div>
      <h2 class="kt-section__title">Escribe <em class="kt-italic">tu nombre</em>. Mira cómo se vería tu página.</h2>
      <p class="kt-section__lede">Es un anticipo de cómo trabajamos: tú nos das el contexto, nosotros lo convertimos en diseño.</p>
    </div>
    <div class="kt-demoLab">
      <div class="kt-demoLab__panel">
        <label class="kt-field">
          <span class="kt-field__lbl kt-mono">// nombre del negocio</span>
          <input id="lab-name" type="text" maxlength="32" value="Tu Negocio" />
        </label>
        <label class="kt-field">
          <span class="kt-field__lbl kt-mono">// frase principal</span>
          <textarea id="lab-tag" rows="3" maxlength="120">Lo que haces, dicho con claridad.</textarea>
        </label>
        <div class="kt-field">
          <span class="kt-field__lbl kt-mono">// industria</span>
          <div class="kt-segmented" id="lab-seg">
            <button class="kt-segmented__btn is-on" data-niche="0">Médicos</button>
            <button class="kt-segmented__btn" data-niche="1">Dentistas</button>
            <button class="kt-segmented__btn" data-niche="2">Abogados</button>
            <button class="kt-segmented__btn" data-niche="3">Ecommerce</button>
          </div>
        </div>
        <div class="kt-demoLab__hint">
          <span>✨ Esto es solo un boceto.</span>
          <span>Tu sitio real lo dibujamos nosotros, contigo.</span>
        </div>
        <a href="#contacto" class="kt-btn kt-btn--primary">Quiero la versión real →</a>
      </div>
      <div class="kt-demoLab__preview">
        <div class="kt-demoFrame">
          <div class="kt-demoFrame__chrome">
            <div class="kt-demoFrame__dots"><i></i><i></i><i></i></div>
            <div class="kt-demoFrame__url"><span id="lab-url">tunegocio.kreatusitioweb.com</span></div>
            <div class="kt-demoFrame__live"><span class="kt-pulse"></span> previsualización</div>
          </div>
          <div class="kt-demoFrame__viewport" id="lab-viewport" style="height:420px"></div>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ═══════════════════ PRECIOS (MercadoPago intacto) ═══════════════════ -->
<Pricing />


<!-- ═══════════════════ TESTIMONIOS ═══════════════════ -->
<section class="kt-test" id="testimonios">
  <div class="kt-shell">
    <div class="kt-section__head">
      <div class="kt-eyebrow"><span class="kt-eyebrow__dot"></span><span class="kt-mono">06 / Testimonios</span></div>
      <h2 class="kt-section__title">Lo que dicen quienes <em class="kt-italic">ya están en línea</em>.</h2>
    </div>
    <div class="kt-test__grid">
      {TESTIMONIOS.map((t, i) => (
        <figure class:list={["kt-quote", `kt-quote--${i % 3}`]}>
          <div class="kt-quote__mark">"</div>
          <blockquote>{t.q}</blockquote>
          <figcaption>
            <span class="kt-quote__avatar"></span>
            <span><strong>{t.a}</strong><br/>{t.r}</span>
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>


<!-- ═══════════════════ FAQ ═══════════════════ -->
<section class="kt-faq" id="faq">
  <div class="kt-shell kt-faq__shell">
    <div class="kt-faq__intro">
      <div class="kt-eyebrow"><span class="kt-eyebrow__dot"></span><span class="kt-mono">07 / FAQ</span></div>
      <h2 class="kt-section__title">Lo que <em class="kt-italic">siempre nos preguntan</em>.</h2>
      <p class="kt-section__lede">¿No ves tu duda? Escríbenos por WhatsApp y te respondemos en minutos.</p>
      <a href="#contacto" class="kt-btn kt-btn--ghost kt-btn--lg" style="margin-top:16px">Hablar con un humano →</a>
    </div>
    <div class="kt-faq__list">
      {FAQ_ITEMS.map((it, i) => (
        <button class="kt-faqItem" data-faq={i}>
          <span class="kt-faqItem__num kt-mono">{String(i+1).padStart(2,"0")}</span>
          <span class="kt-faqItem__q">{it.q}</span>
          <span class="kt-faqItem__icon" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none"><path d="M6 9l6 6 6-6" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </span>
          <span class="kt-faqItem__a">{it.a}</span>
        </button>
      ))}
    </div>
  </div>
</section>


<!-- ═══════════════════ CONTACTO (Formspree intacto) ═══════════════════ -->
<Contact />

</Layout>


<!-- ═══════════════════ SCRIPTS ═══════════════════ -->
<script define:vars={{ NICHES }}>

  /* ── Render niche preview ── */
  function renderNiche(n) {
    return `<div style="width:100%;height:100%;background:${n.bg};color:${n.ink};display:flex;flex-direction:column;font-family:sans-serif;position:relative">
      <div style="display:flex;align-items:center;justify-content:space-between;padding:14px 22px;border-bottom:1px solid rgba(0,0,0,0.06)">
        <div style="display:flex;align-items:center;gap:8px;font-weight:600;font-size:13px">
          <div style="width:18px;height:18px;border-radius:4px;background:${n.primary}"></div>${n.name}
        </div>
        <div style="display:flex;gap:16px;font-size:12px;opacity:0.6"><span>Inicio</span><span>Servicios</span><span>Contacto</span></div>
        <div style="background:${n.primary};color:#fff;padding:6px 12px;border-radius:999px;font-size:11px;font-weight:500">${n.cta}</div>
      </div>
      <div style="padding:28px 22px 14px">
        <div style="font-size:10px;color:${n.primary};text-transform:uppercase;letter-spacing:0.08em;margin-bottom:8px">${n.sub}</div>
        <div style="font-size:22px;line-height:1.1;max-width:90%;margin-bottom:14px">${n.headline}</div>
        <div style="display:flex;gap:8px">
          <div style="background:${n.primary};color:#fff;padding:7px 14px;border-radius:999px;font-size:11px">${n.cta}</div>
          <div style="background:#fff;border:1px solid rgba(0,0,0,0.1);padding:7px 14px;border-radius:999px;font-size:11px">Ver más</div>
        </div>
      </div>
      <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:8px;padding:0 22px">
        ${n.services.map(s => `<div style="background:#fff;border-radius:10px;padding:10px;border:1px solid rgba(0,0,0,0.05)">
          <div style="height:36px;border-radius:6px;background:${n.soft};margin-bottom:8px"></div>
          <div style="font-size:11px;font-weight:600;margin-bottom:4px">${s}</div>
          <div style="height:3px;background:rgba(0,0,0,0.06);border-radius:2px;margin-bottom:3px"></div>
          <div style="height:3px;width:60%;background:rgba(0,0,0,0.06);border-radius:2px"></div>
        </div>`).join('')}
      </div>
      <div style="position:absolute;bottom:14px;right:16px;background:#fff;padding:6px 10px;border-radius:999px;font-size:10px;color:${n.primary};border:1px solid rgba(0,0,0,0.06)">${n.badge}</div>
    </div>`;
  }

  /* ── Hero demo switcher ── */
  let activeNiche = 0;
  let paused = false;
  const viewport  = document.getElementById('demo-viewport');
  const urlEl     = document.getElementById('demo-url');
  const tabs      = document.querySelectorAll('.kt-demoSwitcher__tab');

  function setNiche(i) {
    activeNiche = i;
    if (viewport) viewport.innerHTML = renderNiche(NICHES[i]);
    if (urlEl) urlEl.textContent = NICHES[i].id + '.kreatusitioweb.com';
    tabs.forEach((t, idx) => t.classList.toggle('is-on', idx === i));
  }
  setNiche(0);
  tabs.forEach((t, i) => t.addEventListener('click', () => { setNiche(i); paused = true; }));
  setInterval(() => { if (!paused) setNiche((activeNiche + 1) % NICHES.length); }, 4200);

  /* ── Demo Lab ── */
  const labName     = document.getElementById('lab-name');
  const labTag      = document.getElementById('lab-tag');
  const labViewport = document.getElementById('lab-viewport');
  const labUrl      = document.getElementById('lab-url');
  const labSeg      = document.getElementById('lab-seg');
  let labNiche = 0;

  function updateLab() {
    const n    = NICHES[labNiche];
    const name = labName?.value || 'Tu Negocio';
    const tag  = labTag?.value  || 'Lo que haces, dicho con claridad.';
    if (labUrl) labUrl.textContent = name.toLowerCase().replace(/\s+/g, '') + '.kreatusitioweb.com';
    if (labViewport) labViewport.innerHTML = renderNiche({ ...n, name, headline: tag });
  }

  labName?.addEventListener('input', updateLab);
  labTag?.addEventListener('input', updateLab);
  labSeg?.querySelectorAll('.kt-segmented__btn').forEach(btn => {
    btn.addEventListener('click', () => {
      labSeg.querySelectorAll('.kt-segmented__btn').forEach(b => b.classList.remove('is-on'));
      btn.classList.add('is-on');
      labNiche = parseInt(btn.dataset.niche);
      updateLab();
    });
  });
  updateLab();

  /* ── FAQ accordion ── */
  document.querySelectorAll('.kt-faqItem').forEach(btn => {
    btn.addEventListener('click', () => {
      const isOpen = btn.classList.contains('is-open');
      document.querySelectorAll('.kt-faqItem').forEach(b => b.classList.remove('is-open'));
      if (!isOpen) btn.classList.add('is-open');
    });
  });

</script>


<!-- ═══════════════════ ESTILOS ═══════════════════ -->
<style is:global>
  :root {
    --kt-brand:      #1D9E75;
    --kt-brand-deep: #0F6E56;
    --kt-brand-soft: #E6F7F2;
    --kt-brand-ink:  #0F2C24;
    --kt-accent:     #c9a84c;
    --kt-bg:         #faf9f5;
    --kt-bg-warm:    #f4f3ef;
    --kt-card:       #fff;
    --kt-ink:        #181917;
    --kt-ink-2:      #3a3d38;
    --kt-muted:      #7a7d78;
    --kt-line:       #e6e4df;
    --kt-line-strong:#cdc9c2;
    --kt-display:    "Instrument Serif", "Times New Roman", serif;
    --kt-sans:       "Geist", system-ui, sans-serif;
    --kt-mono:       "JetBrains Mono", ui-monospace, monospace;
    --kt-shell:      1280px;
    --kt-radius:     18px;
    --kt-shadow:     0 1px 2px rgba(20,30,28,.04), 0 14px 40px -10px rgba(20,30,28,.10);
    --kt-shadow-lg:  0 30px 80px -20px rgba(20,30,28,.18);
  }

  /* Utils */
  .kt-shell    { max-width:var(--kt-shell); margin:0 auto; padding:0 28px; }
  .kt-mono     { font-family:var(--kt-mono); font-size:12px; letter-spacing:.02em; }
  .kt-italic   { font-family:var(--kt-display); font-style:italic; font-weight:400; color:var(--kt-brand-deep); }
  .kt-underline{ background:linear-gradient(180deg,transparent 64%,rgba(29,158,117,.28) 64% 92%,transparent 92%); padding:0 .12em; }

  /* Buttons */
  .kt-btn { display:inline-flex; align-items:center; justify-content:center; gap:8px; padding:11px 18px; border-radius:999px; border:1px solid transparent; font-family:var(--kt-sans); font-weight:500; text-decoration:none; font-size:14.5px; transition:transform .15s,background .2s,color .2s,border .2s; white-space:nowrap; cursor:pointer; }
  .kt-btn--lg { padding:14px 22px; font-size:15px; }
  .kt-btn--primary { background:var(--kt-brand); color:#fff; }
  .kt-btn--primary:hover { background:var(--kt-brand-deep); transform:translateY(-1px); }
  .kt-btn--ghost { background:transparent; color:var(--kt-ink); border-color:var(--kt-line-strong); }
  .kt-btn--ghost:hover { background:var(--kt-bg-warm); }

  /* Eyebrow */
  .kt-eyebrow { display:inline-flex; align-items:center; gap:10px; font-family:var(--kt-mono); font-size:12px; color:var(--kt-muted); text-transform:uppercase; letter-spacing:.06em; }
  .kt-eyebrow__dot { width:7px; height:7px; border-radius:999px; background:var(--kt-brand); box-shadow:0 0 0 4px rgba(29,158,117,.18); }
  .kt-eyebrow__sep { color:var(--kt-line-strong); }

  /* Section heads */
  .kt-section__head { max-width:760px; margin:0 0 48px; }
  .kt-section__head--row { display:flex; align-items:end; justify-content:space-between; gap:40px; max-width:none; }
  .kt-section__head--row > div { max-width:720px; }
  .kt-section__title { font-family:var(--kt-display); font-weight:400; font-size:clamp(32px,4vw,56px); line-height:1.05; letter-spacing:-.02em; margin:12px 0 16px; color:var(--kt-ink); }
  .kt-section__lede  { font-size:17px; color:var(--kt-ink-2); max-width:580px; margin:0; line-height:1.6; }
  .kt-section__lede--side { max-width:360px; align-self:end; }

  /* ── HERO ── */
  .kt-hero { position:relative; padding:80px 0 0; overflow:hidden; background:var(--kt-bg); }
  .kt-hero__bg { position:absolute; inset:0; z-index:0; pointer-events:none; }
  .kt-hero__grid { position:absolute; inset:0; background-image:linear-gradient(to right,rgba(230,228,223,.7) 1px,transparent 1px),linear-gradient(to bottom,rgba(230,228,223,.7) 1px,transparent 1px); background-size:64px 64px; mask-image:radial-gradient(ellipse 70% 60% at 50% 30%,black,transparent 70%); opacity:.5; }
  .kt-hero__glow { position:absolute; left:50%; top:-10%; transform:translateX(-50%); width:1100px; height:800px; background:radial-gradient(ellipse at center,rgba(29,158,117,.18),transparent 60%); filter:blur(50px); }
  .kt-hero__inner { position:relative; z-index:1; display:grid; grid-template-columns:1.1fr 1fr; gap:48px; align-items:center; padding-bottom:80px; }
  @media(max-width:1020px){ .kt-hero__inner { grid-template-columns:1fr; gap:56px; } }
  .kt-hero__copy { max-width:600px; }
  .kt-hero__title { font-family:var(--kt-display); font-weight:400; font-size:clamp(44px,6vw,88px); line-height:.97; letter-spacing:-.02em; margin:18px 0 22px; color:var(--kt-ink); }
  .kt-hero__lede { font-size:18px; line-height:1.55; max-width:540px; color:var(--kt-ink-2); margin:0 0 28px; }
  .kt-hero__lede em { font-family:var(--kt-display); font-style:italic; color:var(--kt-brand-deep); font-size:1.1em; }
  .kt-hero__ctas { display:flex; flex-wrap:wrap; gap:12px; margin-bottom:32px; }
  .kt-hero__proof { list-style:none; padding:0; margin:0; display:flex; gap:28px; flex-wrap:wrap; }
  .kt-hero__proof li { font-size:13px; color:var(--kt-muted); }
  .kt-hero__proof strong { display:block; font-size:26px; color:var(--kt-ink); line-height:1; margin-bottom:4px; font-weight:400; font-family:var(--kt-display); }
  .kt-hero__demo { position:relative; }
  .kt-demoCaption { margin-top:16px; text-align:center; color:var(--kt-muted); }

  /* Demo frame */
  .kt-demoFrame { background:#fff; border-radius:18px; overflow:hidden; box-shadow:var(--kt-shadow-lg); border:1px solid var(--kt-line); }
  .kt-demoFrame__chrome { display:flex; align-items:center; gap:14px; padding:12px 16px; background:#f5f4f0; border-bottom:1px solid var(--kt-line); }
  .kt-demoFrame__dots { display:flex; gap:6px; }
  .kt-demoFrame__dots i { width:11px; height:11px; border-radius:999px; display:inline-block; }
  .kt-demoFrame__dots i:nth-child(1){ background:#ff5f57; }
  .kt-demoFrame__dots i:nth-child(2){ background:#febc2e; }
  .kt-demoFrame__dots i:nth-child(3){ background:#28c840; }
  .kt-demoFrame__url { flex:1; display:flex; align-items:center; gap:8px; background:#fff; border:1px solid var(--kt-line); border-radius:8px; padding:6px 12px; font-family:var(--kt-mono); font-size:12px; color:var(--kt-ink-2); overflow:hidden; }
  .kt-demoFrame__live { display:inline-flex; align-items:center; gap:6px; font-family:var(--kt-mono); font-size:11px; color:var(--kt-brand-deep); text-transform:uppercase; letter-spacing:.05em; white-space:nowrap; }
  .kt-demoFrame__viewport { height:420px; overflow:hidden; background:#fff; position:relative; }
  .kt-pulse { width:7px; height:7px; border-radius:999px; background:var(--kt-brand); display:inline-block; animation:kt-pulse 1.6s infinite; }
  @keyframes kt-pulse { 0%{box-shadow:0 0 0 0 rgba(29,158,117,.6)} 70%{box-shadow:0 0 0 8px transparent} 100%{box-shadow:0 0 0 0 transparent} }
  .kt-demoSwitcher { display:flex; gap:4px; margin-top:14px; background:rgba(244,243,239,.6); padding:4px; border-radius:999px; border:1px solid var(--kt-line); }
  .kt-demoSwitcher__tab { flex:1; background:transparent; border:0; padding:9px 12px; border-radius:999px; font-size:13px; font-weight:500; color:var(--kt-ink-2); position:relative; transition:all .2s; cursor:pointer; font-family:var(--kt-sans); }
  .kt-demoSwitcher__tab.is-on { background:#fff; color:var(--kt-brand-deep); box-shadow:0 1px 4px rgba(20,30,28,.08); }
  .kt-demoSwitcher__bar { position:absolute; left:12px; right:12px; bottom:4px; height:2px; background:var(--kt-brand); border-radius:2px; transform:scaleX(0); transform-origin:left; transition:transform .2s; }
  .kt-demoSwitcher__tab.is-on .kt-demoSwitcher__bar { transform:scaleX(1); }

  /* Marquee */
  .kt-marquee { border-top:1px solid var(--kt-line); border-bottom:1px solid var(--kt-line); background:var(--kt-bg-warm); overflow:hidden; padding:16px 0; }
  .kt-marquee__track { display:flex; gap:44px; white-space:nowrap; animation:kt-marquee 40s linear infinite; }
  .kt-marquee__item { font-family:var(--kt-display); font-size:22px; color:var(--kt-ink); display:inline-flex; gap:18px; align-items:center; }
  .kt-marquee__star { color:var(--kt-brand); font-size:16px; }
  @keyframes kt-marquee { from{transform:translateX(0)} to{transform:translateX(-50%)} }

  /* ── NICHOS ── */
  .kt-nichos { padding:120px 0; background:var(--kt-bg); }
  .kt-nichos__grid { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; }
  @media(max-width:1020px){ .kt-nichos__grid { grid-template-columns:repeat(2,1fr); } }
  @media(max-width:600px) { .kt-nichos__grid { grid-template-columns:1fr; } }
  .kt-nicho { background:var(--kt-card); border:1px solid var(--kt-line); border-radius:var(--kt-radius); padding:28px 24px; position:relative; overflow:hidden; transition:transform .25s,border-color .25s,box-shadow .25s; display:block; text-decoration:none; color:inherit; cursor:pointer; }
  .kt-nicho:hover { transform:translateY(-4px); border-color:var(--kt-brand); box-shadow:var(--kt-shadow); }
  .kt-nicho__num { font-size:28px; margin-bottom:24px; }
  .kt-nicho__title { font-family:var(--kt-display); font-weight:400; font-size:24px; margin:0 0 12px; }
  .kt-nicho__body { font-size:14.5px; color:var(--kt-ink-2); margin:0 0 18px; line-height:1.55; }
  .kt-nicho__tag { color:var(--kt-muted); }
  .kt-nicho__arrow { position:absolute; top:24px; right:24px; width:36px; height:36px; border-radius:999px; background:var(--kt-brand-soft); color:var(--kt-brand-deep); display:flex; align-items:center; justify-content:center; transition:transform .25s,background .25s; }
  .kt-nicho:hover .kt-nicho__arrow { background:var(--kt-brand); color:#fff; }

  /* ── BENTO ── */
  .kt-bento { padding:60px 0 120px; background:var(--kt-bg); }
  .kt-bento__grid { display:grid; grid-template-columns:repeat(6,1fr); grid-auto-rows:minmax(180px,auto); gap:16px; }
  .kt-bento__cell { background:var(--kt-card); border:1px solid var(--kt-line); border-radius:var(--kt-radius); padding:28px; display:flex; flex-direction:column; gap:14px; }
  .kt-bento__cell--big { grid-column:span 3; grid-row:span 2; background:linear-gradient(180deg,var(--kt-card),var(--kt-brand-soft)); }
  .kt-bento__cell:nth-child(2){ grid-column:span 3; }
  .kt-bento__cell:nth-child(3){ grid-column:span 2; }
  .kt-bento__cell:nth-child(4){ grid-column:span 4; background:var(--kt-brand-deep); color:#fff; border-color:var(--kt-brand-deep); }
  .kt-bento__cell--wide { grid-column:span 6; }
  .kt-bento__cell h3 { font-family:var(--kt-display); font-weight:400; font-size:28px; line-height:1.05; margin:0; }
  .kt-bento__cell h3 em { font-style:italic; color:var(--kt-brand-deep); }
  .kt-bento__cell:nth-child(4) h3 em { color:var(--kt-accent); }
  .kt-bento__cell p { font-size:14.5px; color:var(--kt-ink-2); margin:0; line-height:1.55; }
  .kt-bento__cell:nth-child(4) p { color:rgba(255,255,255,.75); }
  .kt-bento__head { display:flex; flex-direction:column; gap:8px; }
  .kt-bento__cell .kt-mono { color:var(--kt-muted); }
  .kt-bento__cell:nth-child(4) .kt-mono { color:rgba(255,255,255,.55); }
  @media(max-width:1020px){ .kt-bento__grid { grid-template-columns:repeat(2,1fr); } .kt-bento__cell,.kt-bento__cell--big,.kt-bento__cell--wide { grid-column:span 2!important; grid-row:auto!important; } }
  .kt-perf { display:flex; flex-direction:column; gap:12px; margin-top:auto; }
  .kt-perf__row { display:grid; grid-template-columns:50px 1fr 70px; align-items:center; gap:12px; font-size:12px; color:var(--kt-ink-2); }
  .kt-perf__bar { height:6px; background:rgba(230,228,223,.7); border-radius:999px; overflow:hidden; }
  .kt-perf__bar i { display:block; height:100%; background:var(--kt-brand); border-radius:999px; }
  .kt-search { margin-top:auto; }
  .kt-search__bar { background:var(--kt-bg-warm); padding:10px 14px; border-radius:10px; font-size:13px; color:var(--kt-ink-2); border:1px solid var(--kt-line); }
  .kt-search__result { background:var(--kt-card); padding:10px 14px; border-radius:10px; font-size:13px; margin-top:6px; border:1px solid var(--kt-line); }
  .kt-search__result strong { color:var(--kt-brand-deep); }
  .kt-search__result span { color:var(--kt-muted); }
  .kt-devices { display:flex; gap:12px; align-items:flex-end; margin-top:auto; }
  .kt-devices__phone { width:56px; height:100px; border:2px solid var(--kt-line-strong); border-radius:10px; padding:6px; background:var(--kt-bg-warm); }
  .kt-devices__phone > div:first-child { height:30px; background:var(--kt-brand-soft); border-radius:4px; margin-bottom:6px; }
  .kt-devices__line { height:4px; background:var(--kt-line-strong); border-radius:2px; margin-bottom:4px; }
  .kt-devices__line.short { width:60%; }
  .kt-devices__tablet { width:100px; height:80px; border:2px solid var(--kt-line-strong); border-radius:8px; padding:8px; background:var(--kt-bg-warm); display:flex; flex-direction:column; gap:4px; }
  .kt-devices__tablet > div { height:14px; background:var(--kt-brand-soft); border-radius:3px; }
  .kt-devices__tablet > div:nth-child(2){ background:var(--kt-line-strong); }
  .kt-devices__tablet > div:nth-child(3){ background:var(--kt-line-strong); width:70%; }
  .kt-chat { margin-top:auto; display:flex; flex-direction:column; gap:6px; }
  .kt-chat__msg { padding:8px 12px; border-radius:14px; font-size:13px; max-width:85%; }
  .kt-chat__msg--in  { background:rgba(255,255,255,.12); color:#fff; align-self:flex-start; border-top-left-radius:4px; }
  .kt-chat__msg--out { background:var(--kt-accent); color:var(--kt-brand-deep); align-self:flex-end; border-top-right-radius:4px; font-weight:500; }
  .kt-stats { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin-top:8px; }
  .kt-stat { padding:18px 0; border-top:1px solid var(--kt-line); }
  .kt-stat__num { font-family:var(--kt-display); font-size:52px; line-height:1; color:var(--kt-brand-deep); font-weight:400; letter-spacing:-.02em; }
  .kt-stat__lbl { font-size:13px; color:var(--kt-muted); margin-top:8px; }
  @media(max-width:720px){ .kt-stats { grid-template-columns:repeat(2,1fr); } }

  /* ── PROCESO ── */
  .kt-proc { padding:120px 0; background:var(--kt-bg-warm); border-top:1px solid var(--kt-line); border-bottom:1px solid var(--kt-line); }
  .kt-proc__rail { display:grid; grid-template-columns:repeat(4,1fr); gap:24px; position:relative; padding-top:70px; }
  .kt-proc__line { position:absolute; top:90px; left:0; right:0; height:1px; background:linear-gradient(to right,transparent,var(--kt-line-strong) 8%,var(--kt-line-strong) 92%,transparent); }
  .kt-procStep { position:relative; padding:0 8px; }
  .kt-procStep__node { position:absolute; top:-56px; left:8px; width:44px; height:44px; border-radius:999px; background:var(--kt-brand); color:#fff; display:flex; align-items:center; justify-content:center; font-size:18px; box-shadow:0 0 0 6px var(--kt-bg-warm),0 0 0 7px var(--kt-line); }
  .kt-procStep__num { color:var(--kt-brand-deep); margin-bottom:12px; }
  .kt-procStep__title { font-family:var(--kt-display); font-weight:400; font-size:26px; margin:0 0 10px; }
  .kt-procStep__body { font-size:14.5px; color:var(--kt-ink-2); margin:0; line-height:1.55; }
  @media(max-width:1020px){ .kt-proc__rail { grid-template-columns:repeat(2,1fr); row-gap:60px; } .kt-proc__line { display:none; } }
  @media(max-width:600px) { .kt-proc__rail { grid-template-columns:1fr; } }

  /* ── DEMO LAB ── */
  .kt-demo { padding:120px 0; background:var(--kt-bg-warm); border-top:1px solid var(--kt-line); border-bottom:1px solid var(--kt-line); }
  .kt-demoLab { display:grid; grid-template-columns:380px 1fr; gap:32px; align-items:start; }
  @media(max-width:1020px){ .kt-demoLab { grid-template-columns:1fr; } }
  .kt-demoLab__panel { background:var(--kt-card); border:1px solid var(--kt-line); border-radius:var(--kt-radius); padding:28px; display:flex; flex-direction:column; gap:20px; }
  .kt-field { display:flex; flex-direction:column; gap:8px; }
  .kt-field__lbl { color:var(--kt-muted); }
  .kt-field input,.kt-field textarea { font-family:var(--kt-sans); font-size:15px; border:1px solid var(--kt-line); border-radius:10px; padding:10px 14px; background:var(--kt-bg); color:var(--kt-ink); resize:none; outline:none; transition:border-color .2s; }
  .kt-field input:focus,.kt-field textarea:focus { border-color:var(--kt-brand); box-shadow:0 0 0 3px rgba(29,158,117,.18); }
  .kt-segmented { display:grid; grid-template-columns:repeat(2,1fr); gap:4px; padding:4px; background:var(--kt-bg-warm); border-radius:12px; border:1px solid var(--kt-line); }
  .kt-segmented__btn { background:transparent; border:0; padding:8px; border-radius:8px; font-size:13px; color:var(--kt-ink-2); cursor:pointer; transition:all .2s; font-family:var(--kt-sans); }
  .kt-segmented__btn.is-on { background:#fff; color:var(--kt-brand-deep); box-shadow:0 1px 4px rgba(20,30,28,.08); font-weight:600; }
  .kt-demoLab__hint { display:flex; flex-direction:column; gap:4px; color:var(--kt-muted); font-size:13px; padding:12px 0; border-top:1px dashed var(--kt-line); border-bottom:1px dashed var(--kt-line); }
  .kt-demoLab__preview .kt-demoFrame__url { font-size:11px; }

  /* ── TESTIMONIOS ── */
  .kt-test { padding:120px 0; background:var(--kt-bg-warm); border-top:1px solid var(--kt-line); }
  .kt-test__grid { display:grid; grid-template-columns:repeat(2,1fr); gap:20px; }
  @media(max-width:720px){ .kt-test__grid { grid-template-columns:1fr; } }
  .kt-quote { background:var(--kt-card); border:1px solid var(--kt-line); border-radius:var(--kt-radius); padding:32px; margin:0; position:relative; }
  .kt-quote--1 { background:var(--kt-brand-soft); border-color:transparent; }
  .kt-quote--2 { background:var(--kt-brand-deep); color:#fff; border-color:var(--kt-brand-deep); }
  .kt-quote__mark { font-family:var(--kt-display); font-size:80px; line-height:.5; color:var(--kt-brand); position:absolute; top:32px; right:24px; opacity:.5; }
  .kt-quote--2 .kt-quote__mark { color:var(--kt-accent); opacity:1; }
  .kt-quote blockquote { font-family:var(--kt-display); font-size:20px; line-height:1.3; margin:0 0 24px; max-width:90%; }
  .kt-quote figcaption { display:flex; align-items:center; gap:12px; font-size:14px; }
  .kt-quote__avatar { width:40px; height:40px; border-radius:999px; background:linear-gradient(135deg,var(--kt-brand),var(--kt-accent)); flex-shrink:0; }
  .kt-quote--2 figcaption { color:rgba(255,255,255,.85); }

  /* ── FAQ ── */
  .kt-faq { padding:120px 0; background:var(--kt-bg); }
  .kt-faq__shell { display:grid; grid-template-columns:1fr 1.4fr; gap:60px; align-items:start; }
  @media(max-width:1020px){ .kt-faq__shell { grid-template-columns:1fr; gap:40px; } }
  .kt-faq__intro { position:sticky; top:100px; }
  .kt-faq__list { display:flex; flex-direction:column; }
  .kt-faqItem { display:grid; grid-template-columns:40px 1fr 24px; gap:14px; align-items:start; background:transparent; border:0; border-top:1px solid var(--kt-line); padding:24px 0; text-align:left; width:100%; cursor:pointer; }
  .kt-faqItem:last-child { border-bottom:1px solid var(--kt-line); }
  .kt-faqItem__num { color:var(--kt-muted); padding-top:4px; font-family:var(--kt-mono); font-size:12px; }
  .kt-faqItem__q { font-family:var(--kt-display); font-size:20px; line-height:1.2; color:var(--kt-ink); font-weight:400; }
  .kt-faqItem__icon { color:var(--kt-ink-2); transition:transform .2s; padding-top:4px; }
  .kt-faqItem.is-open .kt-faqItem__icon { transform:rotate(180deg); color:var(--kt-brand); }
  .kt-faqItem__a { grid-column:2; font-size:15px; color:var(--kt-ink-2); line-height:1.6; max-height:0; overflow:hidden; opacity:0; transition:max-height .3s ease,opacity .25s ease,margin-top .3s ease; margin-top:0; }
  .kt-faqItem.is-open .kt-faqItem__a { max-height:200px; opacity:1; margin-top:12px; }
</style>